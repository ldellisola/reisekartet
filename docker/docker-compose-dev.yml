version: "3"

services:
  mongo:
    image: mongo
    restart: unless-stopped
    ports:
      - 27017:27017

  mongo-express:
    image: mongo-express
    restart: unless-stopped
    ports:
      - 9081:8081
    environment:
      ME_CONFIG_MONGODB_URL: mongodb://mongo:27017/
